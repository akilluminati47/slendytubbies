# Raw Sketchfab downloads - re-fetch with tools/fetch_sketchfab.py.
# Anything the game loads at runtime belongs in assets/game/ instead.
node_modules/
assets/models/

# Local editor/debug config
.claude/

# Baked rig: large, and currently broken (see README "Known issue").
# Rig source, staged locally for the in-browser binder. Not shipped while
# CFG.tubby.useBakedRig is false; re-stage from assets/models when needed.
assets/game/rig/
assets/game/tubbies.glb
assets/game/tubbies.json

# Worker local state
worker/node_modules/
.wrangler/
dist/
